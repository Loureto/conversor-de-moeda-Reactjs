import { render } from "@testing-library/react";
import { CardCurrency } from "./CardCurrency";


describe('CardCurrency component', () =>{
    it('renders correctly', () =>{
        render(<CardCurrency />)
    })
})
